"""Removed — superseded by `test_activity.py` after the unified Activity refactor.

Kept as an empty module because deletion isn't permitted in this environment;
the new test file lives at `core/tests/test_activity.py`.
"""
