"""Removed — replaced by `core.services.activities`.

The slice-1 ActivityLog model + helper were consolidated into the unified
`Activity` model. See `core.services.activities.log_event`.
"""

raise ImportError(
    "core.services.activity was removed. Use core.services.activities."
)
