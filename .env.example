# Django
DJANGO_SECRET_KEY=change-me-to-a-long-random-string
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,.onrender.com

# Supabase Postgres connection string
# Settings -> Database -> Connection string (use the "Transaction" pooler URI for Render)
DATABASE_URL=postgresql://postgres:s2m8x32IDFBVSChU@db.mhvmliwlhavjpdgpdzss.supabase.co:5432/postgres
DATABASE_PASSWORD=s2m8x32IDFBVSChU

# Supabase project URL — used to fetch the JWKS public key for token verification
# (Settings -> API -> Project URL). This is the recommended modern path.
SUPABASE_URL=https://mhvmliwlhavjpdgpdzss.supabase.co

# Legacy HS256 secret. Only needed if your project still uses the legacy JWT secret.
# (Settings -> JWT Keys -> Legacy JWT Secret tab). Leave empty if you use SUPABASE_URL.
SUPABASE_JWT_SECRET=

# CORS (Vercel app URL + local dev)
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend.vercel.app
