# Django
DJANGO_SECRET_KEY=change-me-to-a-long-random-string
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,.onrender.com

# Supabase Postgres connection string
# Settings -> Database -> Connection string (use the "Transaction" pooler URI for Render)
DATABASE_URL=postgresql://postgres:s2m8x32IDFBVSChU@db.mhvmliwlhavjpdgpdzss.supabase.co:5432/postgres
DATABASE_PASSWORD=s2m8x32IDFBVSChU

# Supabase project URL — used to fetch the JWKS public key for token verification
# (Settings -> API -> Project URL). This is the recommended modern path.
SUPABASE_URL=https://mhvmliwlhavjpdgpdzss.supabase.co

# Legacy HS256 secret. Only needed if your project still uses the legacy JWT secret.
# (Settings -> JWT Keys -> Legacy JWT Secret tab). Leave empty if you use SUPABASE_URL.
SUPABASE_JWT_SECRET=

# CORS (Vercel app URL + local dev)
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend.vercel.app

# --- Notifications ---
# Telegram bot — create one with @BotFather and paste the token here.
# BOT_USERNAME is what comes after t.me/ (no @).
TELEGRAM_BOT_TOKEN=8414295870:AAFSqPAmTDcXDU-uS_1C-seoubwJ2N_oXVw
TELEGRAM_BOT_USERNAME=continuuit_bot
# Random string used both as path segment and X-Telegram-Bot-Api-Secret-Token
# header value. Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
TELEGRAM_WEBHOOK_SECRET=GjGf0nybPNQYs682_snFrex5h65RNWqhY9wVgnANcQo

# Twilio (Phase 4 — leave empty until you reach the WhatsApp phase)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=

# Default IANA timezone for new users (used for digest scheduling)
NOTIFICATIONS_DEFAULT_TIMEZONE=America/Mexico_City

# --- AI Assistant ---
# Anthropic API key (https://console.anthropic.com → API Keys)
ANTHROPIC_API_KEY=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
# Override only if you need a different fast model. Defaults to Haiku 4.5.
# ASSISTANT_MODEL_FAST=claude-haiku-4-5-20251001
